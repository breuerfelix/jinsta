export class LoginUtil {

    constructor( ) {}

    public setCookiePath( cookiePath ) {
        if ( cookiePath ) {
            this.cookiePath = cookiePath;
        }
    }

    public createCookie( ) {
    }

    public getCookie( ) {
    }

    public deleteCookie( ) {

}
